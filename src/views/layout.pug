html
  head
    title Aroma Coffee
    block styles
      link(rel="stylesheet", href="/styles.css")
  body 
    include navbar.pug
    main
      if error 
          p(class="error")= error
      if message 
          p(class="message")= message
      block content
    #footer
        p some footer content