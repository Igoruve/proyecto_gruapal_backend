extends ../layout

block content 
    h1 Pedidos
    section(class="order")
        if order
            article 
            h2(class="order-title") Mi cesta
            h3(class="order-id")=order.order_id
            each product in order.products 
                include ../product/productCardOrder.pug
        else 
            p No hay productos disponibles