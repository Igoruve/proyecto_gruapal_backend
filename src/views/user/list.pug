extends ../layout

block content 
    h1 Orders 
    section(class="order-list")
        if order
            each order in order 
                a(href=/order/${order.order_id})
                    include orderCard
                    p= order.order_category.name
        else 
            p There is no order available