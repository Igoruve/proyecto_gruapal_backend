extends ../layout

block content 
    h1 Productos 
    section(class="product-list")
        if products
            each product in products 
                include productCard.pug
                
        else 
            p No hay productos disponibles